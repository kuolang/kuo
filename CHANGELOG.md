## v0.0.1

*1 March 2026*

- Declare variables with type annotations or type inference
- Use primitive types: `int`, `float`, `bool`, `string`
- Perform arithmetic, comparison, and logical operations
- Use `if`/`else`, `while`, and `for` control flow
- Declare and call functions with typed parameters and return values
- Recurse through function calls
- Print values with `print()`
- Report syntax errors with line and column information